# WIOM Control Tower — web app + sync loop
# Jalankan: docker compose up -d
services:
  web:
    build:
      context: .
      target: web-runtime
    ports: ["3000:3000"]
    env_file: .env
    environment:
      WIOM_EMBEDDED_SYNC: "0"
      WIOM_SYNC_REQUIRED: "0"
      DUCKDB_STATE_PATH: /app/db/app_state.duckdb
      WIOM_RUNTIME_CONFIG_DIR: /app/db/runtime-config
    volumes:
      # WAJIB permanen: history + state DuckDB DAN db/runtime-config, tempat
      # setiap konfigurasi yang disimpan dari halaman Pengaturan mendarat.
      - ./db:/app/db
    depends_on:
      sync:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.75"
          memory: 384M

  sync:
    build:
      context: .
      target: sync
    working_dir: /app
    environment:
      SUPERSET_BASE_URL: ${SUPERSET_BASE_URL:-}
      SUPERSET_USERNAME: ${SUPERSET_USERNAME:-}
      SUPERSET_PASSWORD: ${SUPERSET_PASSWORD:-}
      SUPERSET_COOKIE_HEADER: ${SUPERSET_COOKIE_HEADER:-}
      SUPERSET_SESSION_COOKIE: ${SUPERSET_SESSION_COOKIE:-}
      SUPERSET_ACCESS_TOKEN: ${SUPERSET_ACCESS_TOKEN:-}
      DUCKDB_STATE_PATH: /app/db/app_state.duckdb
      WIOM_RUNTIME_CONFIG_DIR: /app/db/runtime-config
    volumes:
      - ./db:/app/db
    command: ["python3", "scripts/superset_to_duckdb.py", "--config", "config/superset-sync.json", "--daemon"]
    healthcheck:
      test:
        - CMD
        - python3
        - -c
        - "import json,time; from datetime import datetime; h=json.load(open('db/.superset-sync-heartbeat.json')); assert h.get('ready') and time.time()-datetime.fromisoformat(h['heartbeat_at']).timestamp()<20"
      start_period: 20s
      interval: 10s
      timeout: 5s
      retries: 6
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "1.00"
          memory: 640M
